#include<stdio.h>
#include<conio.h>
void main()
{
       float rate, qty ,amt , dis, billamt ,gst , netbill;
       clrscr();
       printf("enter rate :");
       scanf("%f",&rate);
       printf("enter qty :");
       scanf("%f",&qty);
       amt=rate*qty;
       dis =(amt*5)/100;
       billamt = amt-dis;
       gst=(billamt*18)/100;
       netbill=billamt+gst;
       printf("rate\tqty\tamt\t\tdis\tbillamt\t\tgstamt\t\tnetbill");
       printf("\n%.2f\t%.2f\t%.2f\t%.2f\t%.2f\t\t%.2f\t\t%.2f\t",rate,qty,amt,dis,billamt,gst,netbill);
       getch();

}